pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

plugins {
    id 'xyz.tynn.convention.settings' version "$xyzTynnConventionVersion"
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include ':bom'

include ':bitmap'

include ':binding'
include ':recycler'

include ':delegate'
include ':keyboard'
include ':flow'
include ':storage'

include ':time'
